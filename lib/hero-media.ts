/** Desktop-only hero background video (see `components/hero-background.tsx`). */
export const HERO_IMAGE_SRC = '/images/hero-boat.jpg'
export const HERO_VIDEO_SRC = '/images/hero-video.mp4'

/** Tailwind `lg` — hero video only on desktop viewports. */
export const HERO_VIDEO_MEDIA_QUERY = '(min-width: 1024px)'

export const HERO_CROSSFADE_MS = 1000

/** Crop zoom on hero video only (image stays at 1). */
export const HERO_VIDEO_ZOOM = 1.8
